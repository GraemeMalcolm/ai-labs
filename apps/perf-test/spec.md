Perf-test is a simple app that tests browser performance.
It consists of a single HTML page and JavaScript file.
When first loaded, the app runs a simple loop of 500 iterations, timing how long it takes to complete the loop. It does this three times, and then reports the average completion time.
